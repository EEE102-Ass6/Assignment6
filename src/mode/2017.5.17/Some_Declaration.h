//header file: some reference for my own tasks.

//structure:
typedef struct user {
	//structure:

};

typedef struct user_node {
	//structure:

	//the link (the next node):
	struct user_node *next;
};